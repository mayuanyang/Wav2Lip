# 面部关键点预计算工具使用说明

## 概述

为了提高 SyncNet 训练的效率，我们创建了一个工具来预计算每张图片的面部关键点（landmarks），避免在训练过程中重复计算。

## 文件说明

1. `precompute_landmarks.py` - 用于预计算并保存面部关键点的脚本
2. `syncnet_dataset.py` - 修改后的数据集文件，支持从预计算的文件中读取关键点

## 使用方法

### 1. 预计算面部关键点

运行以下命令来预计算训练数据中所有图片的面部关键点：

```bash
python precompute_landmarks.py --data_root training_data --num_workers 4
```

参数说明：
- `--data_root`: 训练数据的根目录（默认为 "training_data"）
- `--num_workers`: 使用的进程数（默认为 1）

该脚本会：
- 递归遍历指定目录下的所有子目录
- 自动检测包含 `.jpg` 图片文件的目录
- 对每个 `.jpg` 图片文件计算面部关键点
- 将关键点数据保存为同名的 `.npy` 文件（例如 `0.jpg` 对应 `0.npy`）

### 2. 在训练中使用预计算的关键点

修改后的 `syncnet_dataset.py` 会自动检测并使用预计算的关键点文件：
- 如果存在对应的 `.npy` 文件，直接加载使用
- 如果不存在，则跳过唇部遮罩处理

## 实现细节

### 面部关键点计算

使用 MediaPipe FaceMesh 来检测面部关键点，特别关注嘴唇区域的 30 个关键点。

### 数据存储

关键点数据以 NumPy 数组的形式保存在 `.npy` 文件中，每个文件对应一张图片。

### 训练时的使用

在训练过程中，数据集会自动加载预计算的关键点数据，无需重复计算，从而提高训练效率。
